apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "and-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "and-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "and-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
