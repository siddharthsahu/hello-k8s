apiVersion: v1
kind: Service
metadata:
  name: {{ include "and-frontend.fullname" . }}
  labels:
    {{- include "and-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "and-frontend.selectorLabels" . | nindent 4 }}
