apiVersion: v1
kind: Service
metadata:
  name: {{ include "and-backend.fullname" . }}
  labels:
    {{- include "and-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "and-backend.selectorLabels" . | nindent 4 }}
